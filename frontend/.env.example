# Copy to .env for local dev; set as env vars in your hosting platform for prod.

# Backend API base URL. Leave empty in dev (Vite proxies /api to localhost:8000).
# Set in prod, e.g. https://raise-api.onrender.com/api
VITE_API_URL=
